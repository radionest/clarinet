# Python / UV
.venv/
__pycache__

# PDM (removed - backup in .backup-pdm)
.pdm-python
pdm.lock
.pdm-build/

# IDE
.idea

# Testing
.hypothesis
.coverage
.env.test

# Mutation testing (mutmut) — generated mutant copies + stats
/mutants/

# Build artifacts
**/build/**
dist/
clarinet/static/

# Gleam (generated build cache is under **/build/** above)

# Database
*.db
*.db-shm
*.db-wal
*.db-journal

# Logs
*.log

# Generated data
examples/*/data/

# Alembic setup is a downstream-project concern (see CLAUDE.md / memory)
examples/*/alembic.ini
examples/*/alembic/

# Local alembic artifacts that `clarinet init-migrations` writes at the
# repo root — only relevant when running the framework against a dev
# SQLite DB (e.g. via scripts/seed_dev_data.py); the framework itself
# does not ship migrations.
/alembic.ini
/alembic/

# DICOM
**/dicomweb_cache
**/test_dataset/**

# Misc
.Rhistory
*.docx

# VM deploy artifacts
*.qcow2
*.iso
*-rendered.yaml

# Scratch / planning docs
TODO*.md
todo*.md
docs/*.png
TASKIQ_*.md
REFACTOR.md

# Test RecordType configs (scratch/development)
/tasks/

# Dev notes (centralized storage via symlink)
dev-notes

# Superpowers session artifacts (plans, specs, SDD briefs/reports/diffs)
.superpowers/
docs/superpowers/

# Claude Code local config (settings.local.json, worktrees, runtime dirs)
# Exception: project infra shared via git — rules/, agents/, commands/, hooks/, settings.json
.claude/*
!.claude/rules/
!.claude/agents/
!.claude/commands/
!.claude/hooks/
!.claude/settings.json

# OpenSpec local spec store (Claude working memory, synced via doc-review gate)
/openspec
