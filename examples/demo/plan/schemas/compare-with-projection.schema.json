{
    "type": "object",
    "properties": {
        "false_negative": {
            "type": "array",
            "description": "Defects present on master-model projection but not found by the inspector",
            "items": {
                "type": "object",
                "properties": {
                    "defect_num": {
                        "type": "integer",
                        "description": "ROI number from master model"
                    }
                },
                "required": ["defect_num"]
            }
        },
        "false_negative_num": {
            "type": "integer",
            "description": "Count of false negative defects"
        },
        "false_positive_num": {
            "type": "integer",
            "description": "Count of defects found by inspector but absent on projection. Triggers update_master_model when > 0"
        }
    },
    "required": ["false_negative", "false_negative_num", "false_positive_num"],
    "unevaluatedProperties": false
}
