{
    "type": "object",
    "properties": {
        "false_negative": {
            "type": "array",
            "description": "Lesions present on master-model projection but not found by the doctor",
            "items": {
                "type": "object",
                "properties": {
                    "lesion_num": {
                        "type": "integer",
                        "description": "ROI number from master model"
                    }
                },
                "required": ["lesion_num"]
            }
        },
        "false_negative_num": {
            "type": "integer",
            "description": "Count of false negative lesions"
        },
        "false_positive_num": {
            "type": "integer",
            "description": "Count of lesions found by doctor but absent on projection. Triggers update_master_model when > 0"
        }
    },
    "required": ["false_negative", "false_negative_num", "false_positive_num"],
    "unevaluatedProperties": false
}
