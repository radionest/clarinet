# Clarinet Configuration File

# Server settings
port = 8000
host = "127.0.0.1"
debug = true

# Database settings
database_driver = "sqlite"
database_name = "clarinet"

# Storage settings
storage_path = "./data"

# Security (change in production!)
secret_key = "change-this-secret-key-in-production"

# DICOM settings (local node)
dicom_aet = "CLARINET"
dicom_port = 11112

# Test PACS server (Orthanc on klara)
dicom_pacs_aet = "ORTHANC"
dicom_pacs_host = "192.168.122.151"
dicom_pacs_port = 4242
