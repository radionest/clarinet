# Installed as /opt/clarinet/.env.example on the test VM. Mimics a downstream
# project layout: Quarto's startup dotenv loader aborts on declared-but-unset
# vars unless quarto runs from a neutral cwd (regression for quarto_status).
CLARINET_SECRET_KEY=
CLARINET_DATABASE_PASSWORD=
